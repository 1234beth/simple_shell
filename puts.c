#include <stdio.h>
/**
 * main - print the string in the given function
 *
 * Description: using the main function
 * prints "how about dancing to the solved code errors 
 * Return: 0
 */
int main(void)
{
	puts("\"how about dancing to the solved code errors");
	return (0);
}
